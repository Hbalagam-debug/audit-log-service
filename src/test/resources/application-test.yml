spring:
  datasource:
    url: 'jdbc:sqlite:./target/test-db-test.db'
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always

audit:
  retention:
    window-days: 90
    default-dry-run: true
    require-approval: true
  redaction:
    require-approval: true
    mask-value: '[REDACTED]'
    allow-list: /ipAddress,/deviceId,/device/id,/accountNumber,/personalIdentifier
  encryption:
    enabled: true
    format-version: enc-v1
    algorithm: AES-256-GCM
    sensitive-pointers: /accountNumber,/personalIdentifier
  export:
    max-records: 100
